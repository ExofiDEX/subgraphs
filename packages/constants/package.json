{
  "name": "const",
  "version": "1.0.0",
  "description": "Constants",
  "license": "MIT",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "codegen": "mustache ../../config/$NETWORK.js index.template.ts > index.ts && true",
    "build": "true"
  }
}
