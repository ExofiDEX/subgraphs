{
  "name": "MagneticFieldGenerator",
  "license": "MIT",
  "version": "0.0.1",
  "scripts": {
    "codegen": "mustache ../../config/$NETWORK.js template.yaml > subgraph.yaml && graph codegen",
    "build": "graph build",
    "deploy:goerli": "graph deploy --node https://api.studio.thegraph.com/deploy/ mfg-goerli",
    "create-local": "graph create --node http://localhost:8020/ mfg",
    "remove-local": "graph remove --node http://localhost:8020/ mfg",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mfg"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.1",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
